SELECT BRANCH_ZIP, SUM(TRANSACTION_VALUE) TOTAL_TRANSACTION_VALUE 
FROM CDW_SAPP_D_CREDITCARD CD LEFT JOIN CDW_SAPP_D_BRANCH BR
ON CD.BRANCH_CODE=BR.BRANCH_CODE
GROUP BY BRANCH_ZIP
ORDER BY 2 DESC
LIMIT 20;